# TASKS: แอพเช็คตู้ไข่เจม

- [x] 1. โครงหน้าเว็บ + house style (index.html, css, layout มือถือ, แถบค้นหาบนสุด, ปุ่มเพิ่มตู้)
      พิสูจน์: เปิดในเบราว์เซอร์จำลองมือถือ (375x812) เห็นโครงหน้าครบ ไม่มี error ใน console — ทดสอบแล้ว
- [x] 2. ชั้นข้อมูล IndexedDB (เพิ่ม/แก้/ลบ/อ่านตู้ไข่ + เก็บรูปหลายรูปต่อตู้)
      พิสูจน์: เพิ่มตู้พร้อมรูป 2 รูป แล้ว reload หน้า ข้อมูลและรูปยังอยู่ครบ — ทดสอบแล้ว
- [x] 3. ฟอร์มเพิ่ม/แก้ไขตู้ (ชื่อ, สถานที่, เลขกุญแจ, แนบ/ลบรูปหลายรูป, บันทึก)
      พิสูจน์: เพิ่มตู้พร้อมรูป → ปรากฏในรายการ, แก้ชื่อ → รายการและหน้ารายละเอียดอัปเดตตาม — ทดสอบแล้ว
- [x] 4. ค้นหาแบบใกล้เคียง (filter real-time จากชื่อ, ไม่พบ = ข้อความแจ้ง)
      พิสูจน์: ทดสอบตรงตามตัวอย่างใน SPEC.md ("บิ๊กซี" เจอ 2, "อ่อนนุช" เจอ 1, คำที่ไม่มี → "ไม่พบตู้ที่ค้นหา")
- [x] 5. หน้ารายละเอียด + ลบตู้ (มี confirm ก่อนลบ)
      พิสูจน์: ลบตู้ → หายจากรายการทันที — ทดสอบแล้ว
- [x] 6. Export / Import backup (ไฟล์ JSON รวมรูป base64)
      พิสูจน์: export ไฟล์จริง → import กลับ → รายชื่อและรูปครบทั้ง 3 ตู้ — ทดสอบแล้ว
- [ ] 7. ตรวจ edge case เพิ่มเติมที่ยังไม่ได้ทดสอบ: กดบันทึกซ้ำ ๆ เร็ว ๆ (double-submit), ไฟล์ backup ที่ corrupt/ผิดฟอร์แมต, พื้นที่ IndexedDB เต็มเมื่อมีรูปจำนวนมาก
      (ชื่อ/สถานที่/เลขกุญแจว่าง, ไม่มีรูป, ชื่อซ้ำกัน — ทดสอบแล้วว่าใช้งานได้ตามปกติ)

- [x] 8. แก้ค้นหาให้ครอบคลุมทั้งชื่อตู้และสถานที่ (ของเดิมค้นแค่ชื่อตู้) ตามการใช้งานจริงที่พิมพ์ชื่อสถานที่เป็นหลัก
      พิสูจน์: ทดสอบค้นหาด้วยชื่อสถานที่ ("ร้านพี่เอ็น", "ส้มตำป้าตุ๊") → เจอตู้ที่ตั้งอยู่ที่นั่นถูกต้อง
- [x] 9. เตรียมไฟล์นำเข้าข้อมูลตู้ไข่จริง 58 รายการ (นำเข้าข้อมูลตู้ไข่-เริ่มต้น.json) จากรายการที่เจ้าของแอพให้มา ยังไม่มีเลขกุญแจ/รูป (ต้องกรอกเพิ่มทีหลัง)
      พิสูจน์: import ไฟล์ผ่านเมนู "กู้คืนข้อมูล" แล้วนับได้ 58 รายการ, ค้นหาด้วยชื่อ/สถานที่ทำงานถูกต้อง — ทดสอบในเบราว์เซอร์จำลองแล้ว (ยังไม่ได้ import บนมือถือจริงของผู้ใช้)

- [x] 10. จัดหน้าแรกใหม่ให้เรียบ: ช่องค้นหา + ปุ่มใหญ่ "เพิ่มตู้ไข่ใหม่"/"ดูข้อมูลทั้งหมด" แทนปุ่ม + ลอย, ผลค้นหาแสดงใต้ช่องเมื่อพิมพ์เท่านั้น, ฟอร์มเรียง สถานที่→เลขกุญแจ→รูป, ชื่อตู้ไม่บังคับ
      พิสูจน์: ทดสอบในเบราว์เซอร์จำลองมือถือ — ค้นหา "ส้มตำ" พบ 2, ดูทั้งหมดได้ 58 เรียงตามสถานที่, บันทึกโดยใส่แค่สถานที่+เลขกุญแจผ่าน, ลบแล้วกลับเป็น 58, ไม่มี error ใน console
      ยังไม่ได้ทดสอบ: ปุ่มถ่ายรูปเปิดกล้องบนมือถือจริง (ในเบราว์เซอร์จำลองทดสอบได้แค่การแนบไฟล์)

- [x] 11. เลขกุญแจ = เลขลำดับ 1–58 ในรายการที่เจ้าของส่งมา (เจ้าของยืนยัน 2026-09-10) ใส่ลงไฟล์นำเข้าครบทุกตู้
- [x] 12. โหลดข้อมูลเริ่มต้นอัตโนมัติจากไฟล์ "นำเข้าข้อมูลตู้ไข่-เริ่มต้น.json" เมื่อเปิดแอพ: เครื่องใหม่ (ฐานข้อมูลว่าง) โหลดทั้งชุด, เครื่องที่เคยโหลดชุดเก่า (ยังไม่มีเลขกุญแจ) เติมเฉพาะเลขที่ว่างตาม id ไม่ทับที่ผู้ใช้กรอกเอง
      พิสูจน์: ทดสอบ 3 กรณีในเบราว์เซอร์จำลอง — ฐานข้อมูลเก่าไม่มีเลข → เติมครบ, ฐานข้อมูลว่าง → ได้ 58 พร้อมเลข, กรอกเลขเอง "99" แล้วเปิดใหม่ → ยังเป็น 99
      หมายเหตุ: ใส่ ?v=3 ท้าย app.js/app.css ใน index.html กันเบราว์เซอร์ใช้ไฟล์เก่าที่แคชไว้ ต้องเปลี่ยนเลขทุกครั้งที่แก้โค้ด

- [x] 13. เปลี่ยนไอคอนบนแถบด้านบนเป็นโลโก้ "เจมตะลุยโลก" (จาก รูปโปรไฟล์.jpg ย่อเป็น assets/logo.jpg 240px)
      พิสูจน์: โหลดหน้าแล้วรูปโลโก้แสดงที่มุมซ้ายบน ไม่มี error
- [x] 14. ปรับสีธีมแอพตามแบนเนอร์ "สี Ui.jpg" (เจ้าของสั่ง 2026-09-10): แถบบนฟ้าท้องฟ้า, ปุ่มหลัก/ป้ายเลขกุญแจ/แถบเลขกุญแจสีส้มทอง, ตัวอักษรน้ำเงินเข้ม, พื้นหลังฟ้าอ่อน — override ตัวแปรสีของ skin.css ไว้บนสุดของ app.css
      พิสูจน์: โหลดหน้าแรก/ดูทั้งหมด/รายละเอียด เห็นสีใหม่ครบ ไม่มี error

- [x] 15. ตั้ง git repo ในเครื่อง + commit เวอร์ชันแรก + ตั้ง remote git@github.com:bigboybigzero/tukai-app.git (SSH ทดสอบแล้วเชื่อมได้ในชื่อ bigboybigzero)
- [x] 16. GitHub Pages ออนไลน์แล้ว 2026-09-10: https://bigboybigzero.github.io/tukai-app/
      พิสูจน์: เปิดลิงก์จริงในเบราว์เซอร์จำลองมือถือ — seed 58 ตู้โหลดผ่าน (path ไฟล์ภาษาไทย encode ถูก), ค้นหา "ส้มตำ" เจอ 49/4, โลโก้ขึ้น, ไม่มี error
      ยังไม่ได้ทดสอบ: เปิดบนมือถือจริงของเจ้าของ/เพื่อน (คนละเครือข่าย)
- [ ] 17. ฐานข้อมูลกลาง Firebase Firestore: โค้ด 2 โหมด (LocalStore/CloudStore) เขียนแล้วใน app.js, ไฟล์ config ว่างไว้ที่ assets/firebase-config.js
      พิสูจน์โหมดเครื่อง: ผ่านแล้ว 2026-09-10 — ป้าย "เครื่องนี้", seed 58, ค้นหา "ส้มตำ" 2, เพิ่มตู้พร้อมรูป 2400px → เก็บเป็น jpeg 1280px (9.7KB), ลบรูป, ลบตู้กลับเป็น 58, ไม่มี error
      พิสูจน์โหมดคลาวด์: ยังทดสอบไม่ได้จนกว่าเจ้าของสร้างโปรเจกต์ Firebase แล้วส่ง config มา → ทดสอบ 2 เบราว์เซอร์เห็นข้อมูลตรงกัน, รูปอัปโหลด/ลบ, backup/กู้คืน
      ยังไม่ได้ทดสอบ: การย่อรูปกับไฟล์ HEIC จาก iPhone (ถ้า decode ไม่ได้จะเก็บไฟล์เดิมโดยไม่ย่อ)

## BACKLOG (นอกคาบ ไม่ทำตอนนี้)
- Sync ข้ามเครื่อง / หลายผู้ใช้
- ติดตั้งเป็น PWA (add to home screen, offline manifest เต็มรูปแบบ)
- แผนที่ปักหมุดสถานที่จริง (GPS)
