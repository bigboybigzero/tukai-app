# แอพเช็คตู้ไข่เจม

## ธุรกิจ / บริบท
เจ้าของเดินสายเติมของ/เช็คตู้ไข่กาชาปองหลายตู้หลายสถานที่ จำไม่ได้ว่าตู้ไหนอยู่ที่ไหน กุญแจเลขอะไร ต้องมีที่บันทึกแล้วค้นหาได้เร็วจากมือถือ

## ผู้ใช้งาน
เจ้าของ + เพื่อนหลายคน เปิดจากลิงก์เดียวกันบนมือถือ ไม่ต้อง login (ขอบเขตเปลี่ยนเมื่อ 2026-09-10 จากเดิม "คนเดียวเครื่องเดียว") → ต้องมีฐานข้อมูลกลางให้ทุกคนเห็นข้อมูลและรูปชุดเดียวกัน

## ขอบเขต (in scope)
- บันทึกตู้ไข่: ชื่อตู้, สถานที่, เลขกุญแจ (ข้อความอิสระทั้งหมด), รูปภาพได้หลายรูปต่อตู้
- ค้นหาตู้จากชื่อแบบใกล้เคียง (พิมพ์บางส่วนของชื่อก็เจอ) แสดงรูป + เลขกุญแจ + สถานที่
- แก้ไข / ลบตู้ที่บันทึกไว้
- ดูรายการตู้ทั้งหมด

## นอกขอบเขต (out of scope)
- ไม่มี server ของตัวเอง ไม่มีระบบ login/สิทธิ์รายคน (ใครมีลิงก์ก็ใช้ได้)
- ไม่เชื่อมระบบสต๊อกหรือระบบขายใด ๆ

## กฎข้อมูล
- 2 โหมดเก็บข้อมูล เลือกอัตโนมัติจาก `assets/firebase-config.js`: ไม่มี config → IndexedDB ในเครื่อง (ใครเครื่องมัน), มี config → Firebase Firestore เป็นฐานข้อมูลกลาง (real-time ทุกเครื่อง)
- โหมดคลาวด์ใช้ Firestore อย่างเดียว ไม่ใช้ Firebase Storage (Storage รุ่นใหม่บังคับผูกบัตรเครดิต) รูปย่อไม่เกิน 1280px เก็บเป็น dataURL ใน collection `photos/{pid}` แยกจาก `cabinets/{id}` ที่เก็บแค่ thumb 260px เพื่อไม่เปลืองโควตาอ่านตอนโหลดรายการ (จำกัด 1MB/เอกสาร)
- Firestore rules เปิด read/write ทุกคน (ลิงก์ = สิทธิ์ใช้งาน) ตามที่เจ้าของต้องการส่งลิงก์ให้เพื่อนโดยไม่ต้อง login → ต้องมีปุ่ม export/import ไว้สำรองเสมอ
- เผยแพร่ผ่าน GitHub Pages: repo `bigboybigzero/tukai-app` (push ผ่าน SSH ได้จากเครื่องนี้) ลิงก์ https://bigboybigzero.github.io/tukai-app/ — ขั้นตอนที่เจ้าของต้องกดเองอยู่ใน `วิธีเอาขึ้นออนไลน์.md`
- ฟิลด์ชื่อตู้/สถานที่/เลขกุญแจ เป็นข้อความอิสระ ไม่บังคับรูปแบบ
- รูปภาพเก็บเป็นไฟล์ในเครื่อง ไม่อัปโหลดขึ้นที่ไหน
- เลขกุญแจของตู้ 58 ตู้แรก = เลขลำดับ 1–58 ตามรายการที่เจ้าของส่งมา (ไม่ใช่เลขสุ่ม)
- ไฟล์ `นำเข้าข้อมูลตู้ไข่-เริ่มต้น.json` ในโฟลเดอร์คือข้อมูลตั้งต้น แอพโหลดให้อัตโนมัติตอนเปิด (เครื่องว่าง → ทั้งชุด, เครื่องเก่า → เติมเฉพาะเลขกุญแจที่ว่างตาม id) ห้ามเปลี่ยน id ในไฟล์นี้ ไม่งั้นการเติมจะไม่ตรงตู้
- แก้โค้ด app.js/app.css แล้วต้องเพิ่มเลข `?v=` ใน index.html ด้วย ไม่งั้นมือถือที่เคยเปิดจะใช้ไฟล์เก่า

## วิธีเปิดและทดสอบ
เป็น static web app ไม่มี build step:
1. เปิดไฟล์ `index.html` ด้วยเบราว์เซอร์ (หรือรันเซิร์ฟเวอร์ static เช่น `python3 -m http.server` แล้วเข้าทาง `http://localhost:PORT`)
2. ทดสอบบนมือถือ: เปิด URL เดียวกันในเบราว์เซอร์มือถือ, ลองเพิ่มตู้ใหม่พร้อมรูป, ค้นหาด้วยคำบางส่วนของชื่อ, แก้ไข/ลบ
