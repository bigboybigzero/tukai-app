# SPEC: แอพเช็คตู้ไข่เจม

## งานหลัก
ค้นหาตู้ไข่จากชื่อ (พิมพ์บางส่วนก็เจอ) เพื่อรู้ว่าตู้นี้อยู่สถานที่ไหน เลขกุญแจอะไร พร้อมรูปให้จำได้

## ข้อมูล: ตู้ไข่ (cabinet)
| ฟิลด์ | ชนิด | บังคับ | หมายเหตุ |
|---|---|---|---|
| id | string (uuid) | auto | สร้างอัตโนมัติ |
| name | ข้อความอิสระ | ไม่* | ลักษณะ/ชื่อตู้ เช่น "ตู้ 2 ชั้น", "ตู้หัวกลมเหลือง" |
| location | ข้อความอิสระ | ไม่* | สถานที่/ชื่อร้าน เช่น "ส้มตำป้าตุ๊" (*ต้องมี name หรือ location อย่างน้อย 1 อย่าง) |
| keyNumber | ข้อความอิสระ | ไม่ | เลขกุญแจ เช่น "K-102" |
| photos | array of image blob | ไม่ | หลายรูปต่อตู้ เก็บใน IndexedDB |
| note | ข้อความอิสระ | ไม่ | บันทึกเพิ่มเติม (กันไว้เผื่อใช้) |
| createdAt / updatedAt | timestamp | auto | |

ไม่มีความสัมพันธ์กับ entity อื่น (entity เดียวโดด ๆ)

## ฟีเจอร์และเกณฑ์รับงาน

### 0. หน้าแรก (เรียบที่สุด)
- บนสุดคือช่องค้นหา ใต้ช่องมีปุ่มใหญ่ 2 ปุ่ม: "+ เพิ่มตู้ไข่ใหม่" และ "ดูข้อมูลทั้งหมด (N ตู้)"
- ยังไม่พิมพ์อะไร → ไม่แสดงรายการใด ๆ (ไม่รก) พอเริ่มพิมพ์ → ปุ่มหลักซ่อน ผลลัพธ์แสดงด้านล่างช่องค้นหาทันที มีปุ่ม ✕ ล้างคำค้น
- "ดูข้อมูลทั้งหมด" → แสดงทุกตู้เรียงตามสถานที่ (ตู้ที่ไม่ระบุสถานที่ไปท้ายสุด) มีปุ่ม "กลับหน้าแรก" และ "+ เพิ่มตู้" แตะรายการใดก็ได้เพื่อดู/แก้ไข

### 1. เพิ่มตู้ใหม่
- ฟอร์มเรียงตามลำดับที่ใช้จริง: สถานที่/ชื่อร้าน → เลขกุญแจ → ปุ่ม "ถ่ายรูป / เลือกรูป" (หลายรูป) → ลักษณะ/ชื่อตู้ → บันทึกเพิ่มเติม
- ไม่บังคับชื่อตู้ แต่ต้องกรอกสถานที่หรือชื่อตู้อย่างน้อย 1 อย่าง ไม่งั้นแจ้งเตือนและไม่บันทึก
- กด "บันทึก" แล้วตู้ปรากฏในรายการทันที โดยไม่ต้อง reload
- ตัวอย่าง: กรอกชื่อ "ตู้บิ๊กซีอ่อนนุช", สถานที่ "หน้าประตู 2", เลขกุญแจ "A7", แนบรูป 2 รูป → บันทึกสำเร็จ เห็นในรายการพร้อมรูปแรกเป็น thumbnail

### 2. ค้นหาแบบใกล้เคียง (จากชื่อตู้ **หรือ** สถานที่)
- ช่องค้นหาด้านบน พิมพ์คำใด ๆ ที่เป็นส่วนหนึ่งของ "ชื่อตู้" หรือ "สถานที่" (case-insensitive, ไม่สนตำแหน่ง) → กรองรายการแบบ real-time ทุกครั้งที่พิมพ์
- ใช้งานจริง: ผู้ใช้มักจำสถานที่ได้มากกว่าชื่อตู้ จึงพิมพ์ชื่อร้าน/สถานที่เพื่อหาว่าตู้ที่นั่นเลขกุญแจอะไร รูปเป็นแบบไหน
- ผลลัพธ์แต่ละรายการแสดง: รูปตู้ (รูปแรกถ้ามีหลายรูป), ชื่อตู้, สถานที่ (ตัวหนา), เลขกุญแจ
- ตัวอย่าง: พิมพ์ "ร้านพี่เอ็น" (สถานที่) → เจอตู้ที่ตั้งอยู่ร้านพี่เอ็น. มีตู้ชื่อ "ตู้บิ๊กซีอ่อนนุช" และ "ตู้บิ๊กซีลาดพร้าว" พิมพ์ "บิ๊กซี" (ชื่อตู้) → เจอทั้งสองรายการ
- ถ้าไม่พบ → แสดงข้อความ "ไม่พบตู้ที่ค้นหา"

### 3. ดูรายละเอียด / แก้ไข / ลบ
- แตะรายการ → เปิดหน้ารายละเอียดเห็นรูปทั้งหมด, ชื่อ, สถานที่, เลขกุญแจ, บันทึก
- แก้ไขฟิลด์ใดก็ได้แล้วบันทึกทับได้ ลบรูปที่ไม่ต้องการออกได้ เพิ่มรูปใหม่ได้
- ลบตู้ได้ (มีถามยืนยันก่อนลบจริง)

### 4. สำรอง/กู้คืนข้อมูล
- ปุ่ม export ข้อมูลทั้งหมด (รวมรูป) เป็นไฟล์เดียวเพื่อสำรอง
- ปุ่ม import ไฟล์ backup กลับเข้าแอพ (เครื่องใหม่หรือหลังล้างข้อมูล)

## Non-functional
- ใช้งานบนหน้าจอมือถือเป็นหลัก (ปุ่ม/ช่องกรอกขนาดกดง่ายด้วยนิ้ว)
- ทำงานได้แบบออฟไลน์ (ไม่ต้องพึ่ง network หลังโหลดหน้าแรก)
- ข้อมูลอยู่ในเครื่องเท่านั้น (ตาม CLAUDE.md)
