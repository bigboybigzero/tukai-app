# วิธีเอาแอพเช็คตู้ไข่ขึ้นออนไลน์ (ทำครั้งเดียว)

มี 2 ส่วนที่ต้องกดเองในเว็บ เพราะต้องล็อกอินด้วยบัญชีของเจ้าของ (ผู้ช่วยทำแทนไม่ได้)
ทำเสร็จส่วนไหน บอกผู้ช่วยได้เลย ที่เหลือผู้ช่วยทำต่อให้

---

## ส่วน A — ให้ได้ลิงก์เว็บ (GitHub Pages)  ⏱ ประมาณ 3 นาที

เครื่องนี้เชื่อมกับบัญชี GitHub ชื่อ **bigboybigzero** ไว้แล้ว โค้ดพร้อมส่งขึ้นทันที เหลือแค่สร้าง "ที่เก็บ" (repository) ว่าง ๆ รอรับ

1. เปิด https://github.com/new (ล็อกอินบัญชี bigboybigzero)
2. ช่อง **Repository name** พิมพ์ว่า: `tukai-app`
3. เลือก **Public** (ต้องเป็น Public ถึงจะเปิดเว็บฟรีได้)
4. **ไม่ต้องติ๊ก** "Add a README file"
5. กดปุ่มเขียว **Create repository**
6. บอกผู้ช่วยว่า "สร้าง repo แล้ว" → ผู้ช่วยจะส่งโค้ดขึ้นให้ (push)
7. หลังผู้ช่วยส่งโค้ดแล้ว เข้า https://github.com/bigboybigzero/tukai-app/settings/pages
   - **Source** เลือก "Deploy from a branch"
   - **Branch** เลือก `main` และโฟลเดอร์ `/ (root)` → กด **Save**
8. รอ 1–2 นาที แล้วเปิดลิงก์นี้บนมือถือ: **https://bigboybigzero.github.io/tukai-app/**
   (ส่งลิงก์นี้ให้เพื่อนได้เลย)

หมายเหตุ: ตอนนี้ลิงก์จะใช้งานได้ทันที แต่ข้อมูลยัง "อยู่ในเครื่องใครเครื่องมัน" จนกว่าจะทำส่วน B

---

## ส่วน B — ฐานข้อมูลกลาง ให้ทุกคนเห็นข้อมูลและรูปชุดเดียวกัน (Firebase ฟรี)  ⏱ ประมาณ 5 นาที

ใช้ Firestore ของ Google ฟรี (ไม่ต้องผูกบัตร) รูปจะถูกย่อแล้วเก็บในฐานข้อมูลเลย

1. เปิด https://console.firebase.google.com แล้วล็อกอินด้วยบัญชี Google
2. กด **Create a project** (หรือ "สร้างโปรเจ็กต์")
   - ชื่อโปรเจกต์: `tukai` → Continue
   - **ปิด** Google Analytics (ไม่จำเป็น) → Create project → รอสักครู่ → Continue
3. เมนูซ้าย **Build** → **Firestore Database** → กด **Create database**
   - Location เลือก `asia-southeast1 (Singapore)` → Next
   - เลือก **Start in test mode** → Create (หรือ Enable)
4. ตั้งกฎให้ใช้ได้ถาวร (test mode จะหมดอายุใน 30 วัน): ในหน้า Firestore กดแท็บ **Rules** ลบของเดิมทั้งหมด แล้ววางข้อความนี้ → กด **Publish**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

   (แปลว่า: ใครมีลิงก์ก็อ่าน/แก้ได้ — เหมาะกับการส่งลิงก์ให้เพื่อนใช้ ไม่ต้องล็อกอิน แต่ควรกด "สำรองข้อมูล" ในแอพเก็บไฟล์ไว้เป็นระยะ)

5. เอาค่าตั้งค่ามาให้ผู้ช่วย: กดไอคอน **ฟันเฟือง ⚙** ข้าง "Project Overview" มุมซ้ายบน → **Project settings**
   → เลื่อนลงล่างสุดหัวข้อ **Your apps** → กดไอคอน **</>** (Web)
   → ช่อง App nickname พิมพ์ `tukai` → **ไม่ต้องติ๊ก** Firebase Hosting → **Register app**
   → จะเห็นกล่องโค้ดที่มี `const firebaseConfig = { apiKey: "...", ... };`
   → **ก๊อปเฉพาะก้อนในปีกกา { ... }** มาวางในแชทให้ผู้ช่วย
6. ผู้ช่วยจะเอาค่าไปใส่ในไฟล์ `assets/firebase-config.js` แล้วส่งขึ้น GitHub ให้ → เปิดลิงก์ใหม่ ทุกคนจะเห็นข้อมูลชุดเดียวกัน (มุมบนซ้ายของแอพจะขึ้นป้าย "☁ ฐานข้อมูลกลาง")

---

## เมื่อทำครบทั้งสองส่วนแล้ว จะได้

- ลิงก์เดียว เปิดได้ทุกเครื่อง ไม่ต้องติดตั้ง ไม่ต้องล็อกอิน
- ใครเพิ่มตู้ / แก้เลขกุญแจ / ใส่รูป จากเครื่องไหน ทุกเครื่องเห็นตามทันที
- ข้อมูล 58 ตู้ตั้งต้นพร้อมเลขกุญแจ ถูกโหลดเข้าฐานข้อมูลกลางให้อัตโนมัติตอนเปิดครั้งแรก
- ปุ่ม ⋮ มุมขวาบน → "สำรองข้อมูล" ได้ไฟล์ backup รวมรูป เก็บไว้กันข้อมูลหาย
